import React from "react";
import {View, Text, StyleSheet} from 'react-native';

function ClassScreen() {
    return (
        <View>
            <Text style={{fontSize:30, marginTop: 30}}>ClassScreen</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
});

export default ClassScreen;